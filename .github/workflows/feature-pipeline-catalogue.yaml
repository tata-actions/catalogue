name: feature-pipeline-catalogue

on: [push]

jobs:
  feature-pipeline:
    uses: tata-actions/workflows/.github/workflows/feature-nodejs-pipeline.yaml@main
    with:
      project: roboshop
      component: catalogue
      environment: devv@
